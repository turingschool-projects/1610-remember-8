<h2 class='spec-reminder-title' contenteditable="true">{{model.title}}</h2>
<li class='spec-reminder-date' contenteditable="true">{{model.date}}</li>
<li class='spec-reminder-notes' contenteditable="true">{{model.notes}}</li>
<h3>model id {{model.id}}</h3>
{{#link-to "reminders.reminder"}}
  <button {{action "edit"}}>submit</button>
{{/link-to}}
